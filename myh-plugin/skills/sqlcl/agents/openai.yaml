interface:
  display_name: "SQLcl"
  short_description: "Run Oracle SQLcl saved connections"
  default_prompt: "Use $sqlcl to write a SQL script for my Oracle database, save it to a .sql file, run it with a named SQLcl connection, or dump data into an executable SQL export file."
